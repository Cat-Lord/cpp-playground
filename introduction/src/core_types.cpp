
void core_types()
{

  // integer types
  short s;
  signed int a; // signed by default anyways
  signed signed_a;
  unsigned int ua;
  long la;
  long long lla;

  char c;
  unsigned char u_c;
  bool b_t = true; // the size with sizeof(bool) differs based on the implementation
  bool b_f = false;

  // floating numbers
  float f;
  double d;
}